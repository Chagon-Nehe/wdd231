/*
    js/index.js
    Specific JavaScript for the index.html (Home Page).
    Handles any unique interactions or initializations for the home page.
*/

document.addEventListener('DOMContentLoaded', () => {
    // No specific JavaScript interactions are needed for index.html's content
    // beyond what's handled by global CSS for navigation.
    // The CTA button simply links to directory.html, no JS needed for that.
});
